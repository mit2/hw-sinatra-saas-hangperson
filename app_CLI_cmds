$ cucumber features/guess.feature
$ bundle exec rerun -- rackup -p $PORT -o $IP
$ bundle
$ git push heroku master
$ git commit -m "Implemented & tested 'Scenario: game_over. Thus Full Game is Implemented'"
byebug // in the code